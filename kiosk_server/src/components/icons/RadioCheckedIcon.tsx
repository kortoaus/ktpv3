import React from "react";
import { IconBaseProps } from "react-icons";
import { MdRadioButtonChecked } from "react-icons/md";

export default function RadioCheckedIcon({ size, className }: IconBaseProps) {
  return <MdRadioButtonChecked size={size} className={className} />;
}
