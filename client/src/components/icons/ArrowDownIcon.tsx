import React from "react";
import { IconBaseProps } from "react-icons";
import { MdArrowCircleDown } from "react-icons/md";

export default function ArrowDownIcon({ size, className }: IconBaseProps) {
  return <MdArrowCircleDown size={size} className={className} />;
}
