import React from "react";
import { IconBaseProps } from "react-icons";
import { MdRestartAlt } from "react-icons/md";

export default function ResetIcon({ size, className }: IconBaseProps) {
  return <MdRestartAlt size={size} className={className} />;
}
