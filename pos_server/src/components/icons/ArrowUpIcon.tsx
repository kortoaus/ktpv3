import React from "react";
import { IconBaseProps } from "react-icons";
import { MdArrowCircleUp } from "react-icons/md";

export default function ArrowUpIcon({ size, className }: IconBaseProps) {
  return <MdArrowCircleUp size={size} className={className} />;
}
