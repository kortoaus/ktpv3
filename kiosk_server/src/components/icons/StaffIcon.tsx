import React from "react";
import { IconBaseProps } from "react-icons";
import { MdGroup } from "react-icons/md";

export default function StaffIcon({ size, className }: IconBaseProps) {
  return <MdGroup size={size} className={className} />;
}
