import React from "react";
import { IconBaseProps } from "react-icons";
import { MdRadioButtonUnchecked } from "react-icons/md";

export default function RadioUncheckedIcon({ size, className }: IconBaseProps) {
  return <MdRadioButtonUnchecked size={size} className={className} />;
}
