import React from "react";
import { IconBaseProps } from "react-icons";
import { MdTableRestaurant } from "react-icons/md";

export default function TableIcon({ size, className }: IconBaseProps) {
  return <MdTableRestaurant size={size} className={className} />;
}
