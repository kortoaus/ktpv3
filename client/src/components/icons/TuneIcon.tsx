import React from "react";
import { IconBaseProps } from "react-icons";
import { MdTune } from "react-icons/md";

export default function TuneIcon({ size, className }: IconBaseProps) {
  return <MdTune size={size} className={className} />;
}
