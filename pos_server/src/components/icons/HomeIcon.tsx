import React from "react";
import { IconBaseProps } from "react-icons";
import { MdHome } from "react-icons/md";

export default function HomeIcon({ size, className }: IconBaseProps) {
  return <MdHome size={size} className={className} />;
}
